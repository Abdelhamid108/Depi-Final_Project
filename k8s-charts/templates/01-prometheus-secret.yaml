# Kubernetes Secret for storing Prometheus basic authentication credentials
# Used by the ingress or Prometheus UI to enforce HTTP Basic Auth
apiVersion: v1
kind: Secret
metadata:
  name: prometheus-basic-auth
  namespace: amazona
type: Opaque  
 # Generic key-value secret type

stringData:
  # Encoded basic authentication string (username:password)
  # Pulled dynamically from Helm values under monitoring.auth.cred
  auth: {{ .Values.monitoring.auth.cred | quote }}
